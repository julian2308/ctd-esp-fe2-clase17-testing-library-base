import { fireEvent, render, screen } from "@testing-library/react";
import LanguageComponent from './language.component'

describe("verifing the existence of the btns and it's funcionality", () => {

    it("should display the 3 buttons of language", ()=> {
        


    })
})